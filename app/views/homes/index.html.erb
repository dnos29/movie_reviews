<div class="row">
  <% @movies.each do |movie| %>
      <div class="col-md-3">
        <div class="thumbnail">

          <%= link_to (image_tag movie.image.url(:square)), movie %>
        </div>
      </div>
  <% end %>
</div>